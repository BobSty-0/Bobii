namespace bobii_rework.src.GlobalConstants.Interactions
{
    // CommandNames die für die Disabled Commands und UsedFunctions verwendet werden
    public class CommandNames
    {
        public const string whitelistactive = nameof(whitelistactive);
        public const string autotransferowner = nameof(autotransferowner);
        public const string locked = nameof(locked);
    }
}
